# Production environment — used by `npm run build`
# Backend URL: Direct HTTP ALB endpoint.
# Note: Mixed-content warnings expected but requests will work (fetch API allows HTTP from HTTPS).
# TODO: Add HTTPS to ALB to eliminate mixed-content warnings.
#   https://www.chalokisaan.in  → React SPA (Amplify CloudFront)
#   http://chalokisaan-alb-...  → FastAPI backend (direct ALB)
REACT_APP_API_URL=http://chalokisaan-alb-229245969.ap-south-1.elb.amazonaws.com/api
REACT_APP_DEV_BYPASS_AUTH=false
